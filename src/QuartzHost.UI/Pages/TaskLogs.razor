@page "/task/logs"
@page "/task/logs/{info}"
@inherits TaskLogsBase
<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>任务日志</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/">首页</a></li>
                    <li class="breadcrumb-item"><a href="/task/all">任务列表</a></li>
                    <li class="breadcrumb-item active">任务日志</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">任务 @Name 日志列表 @Id</h3>
                    <button type="button" class="btn btn-default btn-sm float-right" @onclick="async ()=> await Load()"><i class="fas fa-sync-alt"></i></button>
                </div>
                <!-- /.card-header -->
                <div class="card-body">
                    <table id="logs" class="table table-bordered table-hover compact">
                        <thead>
                            <tr>
                                <th>编号</th>

                                <th>开始运行时间</th>
                                <th>结束运行时间</th>
                                <th>执行耗时(秒)</th>
                                <th>运行结果</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Results != null && Results.Data != null)
                            {
                                foreach (var log in Results.Data)
                                {
                                    <tr>
                                        <td>@log.TraceId</td>
                                        <td>@log.StartTime.ToString("yyyy-MM-dd HH:mm:ss fff")</td>
                                        <td>@log.EndTime.Value.ToString("yyyy-MM-dd HH:mm:ss fff")</td>
                                        <td>@log.ElapsedTime</td>
                                        <td>@log.ResultName</td>
                                    </tr>
                                }

                            }
                            else
                            {
                                <tr>
                                    <td colspan="7" align="center">Loading...</td>
                                </tr>
                            }
                        </tbody>
                        <PagerInfo Colspan="7" Total="Results?.Data?.FirstOrDefault()?.Total??0" PageIndex="1" PageSize="10" />
                    </table>
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</section>
<!-- /.content -->
@code{

}